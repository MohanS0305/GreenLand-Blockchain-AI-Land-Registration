<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<!-- Console Appender -->
	<appender name="CONSOLE"
		class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>%d{hh:mm:ss a} %-5level [%logger{36}] %M - %msg%n</pattern>
		</encoder>
	</appender>

	<!-- Rolling File Appender with size-based rotation -->
	<appender name="ROLLING_FILE"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>logs/application.log</file>

		<!-- Rolling policy for size-based log rotation -->
		<rollingPolicy
			class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
			<!-- Rotate when the file reaches 10MB and keep logs for each day -->
			<fileNamePattern>logs/application-%d{yyyy-MM-dd}.%i.log
			</fileNamePattern>
			<maxFileSize>10MB</maxFileSize>
			<maxHistory>7</maxHistory> <!-- Keep logs for the last 7 days -->
			<totalSizeCap>100MB</totalSizeCap> <!-- Limit total log size to 100MB -->
		</rollingPolicy>

		<encoder>
			<pattern>%d{hh:mm:ss a} %-5level [%logger{36}] %M - %msg%n</pattern>
		</encoder>
	</appender>

	<!-- Root Logger -->
	<root level="INFO">
		<appender-ref ref="CONSOLE" />
		<appender-ref ref="ROLLING_FILE" />
	</root>
</configuration>
